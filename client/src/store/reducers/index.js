const initialState = {
    something: []
}

const reducer = (state = initialState, action) => {
    switch(action.type) {
        case 'Something':
            default:
                return state
    }
}

export default reducer